- custom = current_page.first? ? {class: 'prev disabled', url: '#'} : {class: 'prev', url: url}
%li{class: custom[:class]}
  = link_to raw(t 'views.pagination.first'), custom[:url], :remote => remote
