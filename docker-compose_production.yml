services:
  app:
    container_name: togogenome_production
    build:
      context: .
      dockerfile: Dockerfile
    image: togogenome_production
    user: ${UID:-0}:${GID:-0}
    environment:
      RAILS_ENV: production
    volumes:
      - .:/app
    ports:
      - "13000:3000"
    # デフォルトコマンドを上書きしたい場合 (例: アセットプリコンパイル後に起動)
    command: bash -c "
      cp config/endpoint_production.yml config/endpoint.yml && 
      cp config/stanza_production.yml config/stanza.yml && 
      bundle exec rake assets:precompile &&
      bundle exec rails server -b 0.0.0.0 -p 3000"